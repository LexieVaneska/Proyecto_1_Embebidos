[package]
name = "detector_ojos"
version = "0.1.0"
edition = "2021"

[dependencies]
opencv = { version = "0.92", default-features = false, features = ["clang-runtime", "face", "highgui", "imgproc", "objdetect", "videoio"] }
